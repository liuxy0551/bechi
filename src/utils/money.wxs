
      var money = {
        getMoney: function (num) {
          if (!num) return ''
          return Number(num).toFixed(2)
        }
      }

      module.exports = {
        getMoney: money.getMoney
      }
